@model LibraryManagementSystem.Models.Category

@{
    ViewData["Title"] = "加入新分類";
}

<h2>@ViewData["Title"]</h2>

<form asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="CategoryName" class="control-label">分類名稱</label>
        <input asp-for="CategoryName" class="form-control" />
        <span asp-validation-for="CategoryName" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="ParentCategoryId" class="control-label">父級分類</label>
        <select asp-for="ParentCategoryId" class="form-control" asp-items='@new SelectList(ViewBag.Categories, "CategoryId", "CategoryName")'>
            <option value="">（無）</option>
        </select>
        <span asp-validation-for="ParentCategoryId" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">保存</button>
    <a asp-action="Index" class="btn btn-secondary">返回列表</a>
</form>
